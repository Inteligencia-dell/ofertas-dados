name: Piloto Claro - 20 cidades

on:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: piloto-claro-20-cidades
  cancel-in-progress: false

jobs:
  testar:
    runs-on: ubuntu-24.04
    timeout-minutes: 45
    steps:
      - name: Obter repositorio
        uses: actions/checkout@v4

      - name: Preparar Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Instalar Playwright e Chromium
        run: |
          python -m pip install --upgrade pip
          pip install playwright
          python -m playwright install --with-deps chromium

      - name: Validar piloto
        run: |
          test -f scripts/coletar_claro_20_cidades_piloto.py
          python -m py_compile scripts/coletar_claro_20_cidades_piloto.py

      - name: Testar 20 cidades
        run: python scripts/coletar_claro_20_cidades_piloto.py

      - name: Resumo
        if: always()
        run: |
          if [ -f dados/piloto-claro-20-cidades.json ]; then
            python - <<'PY' >> "$GITHUB_STEP_SUMMARY"
          import json
          d=json.load(open('dados/piloto-claro-20-cidades.json',encoding='utf-8'))
          s=d.get('summary',{})
          print('### Piloto Claro - 20 cidades')
          print('')
          print('| Indicador | Valor |')
          print('|---|---:|')
          print('| Cidades | %s |' % s.get('cities',0))
          print('| Localizacao confirmada | %s |' % s.get('confirmed',0))
          print('| Com ofertas | %s |' % s.get('withOffers',0))
          print('| Pendentes | %s |' % s.get('pending',0))
          print('')
          for r in d.get('results',[]):
              precos=', '.join('%sM R$ %.2f' % (o.get('speed',0),o.get('price',0)) for o in r.get('offersFound',[])) or '-'
              print('- **%s/%s** - confirmado `%s` - ofertas `%s` - %s - %s' % (
                  r.get('city'),r.get('uf'),r.get('locationConfirmed'),
                  len(r.get('offersFound',[])),precos,r.get('reason',r.get('status'))))
          PY
          else
            echo 'Resultado nao gerado.' >> "$GITHUB_STEP_SUMMARY"
          fi
        shell: bash

      - name: Publicar evidencias
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: piloto-claro-20-cidades-evidencias
          path: |
            dados/piloto-claro-20-cidades.json
            evidencias-claro-20-cidades/
          if-no-files-found: warn
          retention-days: 7
